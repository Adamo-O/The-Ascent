using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MushroomRunTrigger : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
    /*void OnCollisionEnter2D(Collision2D collision)
    {
        if (collision.gameObject.tag == "Player")
        {
            Vector2 velocity = new Vector2((transform.position.x - player.position.x) / (transform.position.x - player.position.x).Length() * speed, (transform.position.y - player.position.y) / (transform.position.y - player.position.y).Length() * speed);
            rigidbody2D.velocity = velocity;
        }
    }*/
}
